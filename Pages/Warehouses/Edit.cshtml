@page
@model OMS.Pages.Warehouses.EditModel
@{
    ViewData["Title"] = "Sửa Kho hàng";
}

<div class="glass-card" style="max-width:600px;margin:0 auto;">
    <div class="mb-4">
        <a href="/Warehouses" style="color:var(--text-muted);font-size:0.85rem;text-decoration:none;">← Danh sách kho</a>
        <h1 class="page-title" style="margin-top:8px;">✏️ Sửa Kho hàng</h1>
    </div>

    <form method="post">
        <input type="hidden" asp-for="Warehouse.Id" />
        <input type="hidden" asp-for="Warehouse.CreatedAt" />

        <div class="form-group">
            <label asp-for="Warehouse.Name" class="form-label"></label>
            <input asp-for="Warehouse.Name" class="form-input" placeholder="Tên kho hàng..." />
            <span asp-validation-for="Warehouse.Name" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Warehouse.Description" class="form-label"></label>
            <input asp-for="Warehouse.Description" class="form-input" placeholder="Mô tả ngắn (tuỳ chọn)..." />
            <span asp-validation-for="Warehouse.Description" class="text-danger"></span>
        </div>

        <div class="grid-2">
            <div class="form-group">
                <label asp-for="Warehouse.SortOrder" class="form-label"></label>
                <input asp-for="Warehouse.SortOrder" type="number" class="form-input" />
                <span asp-validation-for="Warehouse.SortOrder" class="text-danger"></span>
            </div>
            <div class="form-group" style="display:flex;align-items:center;gap:12px;padding-top:28px;">
                <input asp-for="Warehouse.IsActive" type="checkbox" style="width:18px;height:18px;accent-color:#00f2fe;cursor:pointer;" />
                <label asp-for="Warehouse.IsActive" class="form-label" style="margin:0;cursor:pointer;"></label>
            </div>
        </div>

        <div style="display:flex;gap:12px;margin-top:24px;">
            <button type="submit" class="btn-premium" style="flex:1;">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" style="width:17px;height:17px;">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
                </svg>
                Lưu thay đổi
            </button>
            <a href="/Warehouses" class="btn-secondary" style="flex:1;text-align:center;padding:12px;">Hủy</a>
        </div>
    </form>
</div>
